# {{ 页面名称 }}

<!--
【分册纪律】
- 一页一文件（1 页也分册）。页名与需求正文「规格索引」表逐字一致。
- 标 {{#按需}} 的节本期没内容就整节不出现。
- 每页四件齐：元素 / 文案 / 交互 / 业务逻辑——最后一件最易漏。
- 整页复用既有能力的，只写「复用谁 + 上一步给什么 / 下一步产出什么 + 本页特有」。
-->

> **类型**：{{ 页面·改造 / 页面·新页 / 页面·报表 }}
> **端**：{{ PC / H5 / 双端 }}
> **现状基线**：{{ 真实页证据出处；没取证就写「待实查」，别用确定语气 }}

## 本页一屏概览

> 扫读面：读的人 5 秒要带走什么。**先挑支柱，不列全部。**

- **这一页是干什么的**：{{ 一句话 }}
- **本期在这一页改了什么**：{{ ≤3 条，每条一句人话 }}
- **改完之后跟现在最大的不同**：{{ 一句话 }}

## 承载的 User Story

| 编号 | 故事 | 本页承担其中哪几步 |
|---|---|---|
| US-0X | 作为 {{角色}}，我要 {{做什么}}，以便 {{达成什么}} | {{ }} |

## 页面元素 {{#按需：本期有可见元素的增/删/位置或呈现变化才产}}

> **逐元素写展示形态**：不止列"有哪些字段"，逐个写它**在页面哪个位置、长什么样、怎么交互**。
> **平台原本没有的新控件必须专门定义外观 + 行为**（形态、填写态 / 查看态各长啥样），
> 不能只写"一个评分字段"。
> **默认值禁写"待用户配置"**——没有人会改默认设置，默认即最佳实践。

| 字段 / 控件 | 本期 | 位置与形态 | 必填 | 默认值 | 取值 / 校验 | 说明 |
|---|---|---|---|---|---|---|
| {{ 名称 }} | {{ 新增 / 改动 / 不变 }} | {{ 在哪、什么控件 }} | | {{ 有出处才写具体值，否则「沿用平台既有」}} | | |

## 文案（逐字）{{#按需：本期新增或修改了界面文字才产}}

> **逐字抄，禁转述**——这一列有两个下游按字面取用：研发直接复制、定稿时对账。
> 文案的读者是最终用户，不是规格读者：出现"接口 / 字段"类实现语汇 = 语域错了。

| 位置 | 文案原文 | 触发条件 |
|---|---|---|

## 交互说明 {{#按需：本期有交互行为变化才产}}

> **形态：触发动作 → 预期反应**，两半都要有。只写"有个提示"不合格。
> 重要操作要有二次确认与撤回路径；错误提示要能锚点定位，**不光给清单，还要给操作**。

| 触发 | 反应 | 边界 / 异常 |
|---|---|---|

## 业务逻辑

> 规则**分类先行**：先答「这一页要在哪几方面立规则」，得到 3~8 类，
> **第一类固定是「核心口径」**。准入与来源四问见 `../rules/item-admission.md`。
>
> **定义行形态**：`- **BR-XX｜标题**：内容`——一条规则全包只有一行长这样。
> 引用一律写在句中。

### {{ 规则类别一：核心口径 }}

- **BR-01｜{{ 标题 }}**：{{ 内容。有阈值/判定词的，同一条内必须给出数值或明确的取值来源 }}

### {{ 规则类别二：边界 / 叠加 / 异常 }}

## 数据来源 {{#按需：本期新增了数据展示，或某块数据的取数口径变了才产}}

| 展示项 | 数据从哪来 | 怎么算出来的 | 算不出 / 没有值时给什么 |
|---|---|---|---|

## 组件清单 {{#按需：本期引入新组件，或要改造既有组件才产}}

> 组件名**保留原名 + 补业务术语括号**，如 `Table（数据表格）`。业务术语查不到挂占位，禁自拟。

## 异常处理 {{#按需：本期有需要产品定义处置方式的异常才产}}

| 异常场景 | 用户看到什么 | 能怎么办 |
|---|---|---|

## 本页验收场景

> 每条 = 前置 + 操作 + 期望。**期望要可判定**——阈值型的必须给数值或取值来源，否则测试造不出这个场景。

### {{前缀}}-AC1 · {{ 场景简述 }}

- **前置**：{{ }}
- **操作**：{{ }}
- **期望**：{{ }}

## 复用说明 {{#按需：整页或大块复用既有能力时产}}

- **复用谁**：{{ }}
- **上一步给什么 / 下一步产出什么**：{{ }}
- **本页特有**：{{ }}
