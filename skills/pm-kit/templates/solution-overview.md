# 方案概述（四节骨架）

> **一处定义，两处使用**：
> ① 需求载体文件顶部的「📌 需求概览」；② 原型的「需求概述」页。
> **后者直用前者原文，禁转写**——转写必漂移，方案层只有一个信源。

## 节一 · 背景与目标

{{ 为什么要做（真问题，不是表面诉求）+ 要达成什么 }}

## 节二 · 方案与改动

> **核心逻辑等结构化内容进表格，禁散文段落。**

- **落点**：{{ 改在哪些页 / 哪些能力上 }}
- **怎么做**：{{ 一句话方案 }}
- **核心逻辑**：

  | {{ 维度 }} | {{ 规则 }} |
  |---|---|

- **改动点清单**（**页面 + 非页面都列**）：

  | 落点 | 类型 | 改什么 |
  |---|---|---|
  | {{ 页名 / 能力名 }} | {{ 页面·改造 / 非页面·接口 … }} | {{ }} |

> **非页面条目（对外接口、定时任务、平台配置）不占画面，只有在这里能被看到**——
> 漏了就是整单看不见、面客讲不出来。

## 节三 · 用户操作主线

{{ 角色 → 关键步 → 产出，每步标出在哪个页面完成（页名与改动点清单逐字一致，
   客户顺着它就能逐页点过去）。适合用步骤条或时间线呈现。}}

## 节四 · 本期不做 {{#按需：无歧义项则整节不产}}

> 准入：**这一条如果不写，读者真会误以为它在范围内吗？**
> 每条都要指得回一个已存在的事实，指不回的不写。

---

## 准入判据（非页面节 / 全局规则共用）

> **这一条如果不写，测试会不会漏测一个场景？客户会不会在演示会上问"这个怎么办"？**
> 两个都不会 → 不进这一页。

**读者是测试和客户，不是研发**：接口报文字段、配置项编号、日志逐列清单这类**只有研发需要的**，
留在各自规格与技术附录。

## 分量约束

非页面能力每条 4 行：**什么时候发生 / 做什么 / 在哪能看到结果 / 出问题或边界**。
非页面节与全局规则合计超过约 20 行时，**该压缩的是上游规格的能力说明，不是在这里堆**。
